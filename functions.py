# a function  is a container with a few lones of codes  which performs a specific task.
# function ia a block of code which runs only when it is called.
# function is created when we have used same functionality multiple times.

# def first_function():
#     return "hello"
#
#
# result = first_function()
# print(result)


def addition(num_1, num_2):  # num1 and num2 are parameters
    results = num_1 + num_2
     return results

a=addition(4, 5)
print(a)